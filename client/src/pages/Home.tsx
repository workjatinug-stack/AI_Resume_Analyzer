import { useMemo, useRef, useState } from "react";
import { trpc } from "@/lib/trpc";
import { Button } from "@/components/ui/button";
import { Textarea } from "@/components/ui/textarea";
import { Input } from "@/components/ui/input";
import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Progress } from "@/components/ui/progress";
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs";
import { toast } from "sonner";
import { ArrowRight, BarChart3, Check, ChevronDown, FileText, History, Loader2, LockKeyhole, Sparkles, Target, UploadCloud, X, Zap, Search, TrendingUp, AlertCircle, Lightbulb, FileCheck, Menu } from "lucide-react";
import * as pdfjsLib from "pdfjs-dist";
import mammoth from "mammoth/mammoth.browser";
import type { ResumeAnalysis } from "../../../server/resumePrompt";
import { HISTORY_KEY, parseHistory, serializeHistory } from "@/lib/history";

pdfjsLib.GlobalWorkerOptions.workerSrc = new URL("pdfjs-dist/build/pdf.worker.min.mjs", import.meta.url).toString();

const dimensions = [
  ["job_alignment", "Job alignment", "Target fit"],
  ["skills_evidence", "Skills evidence", "Proof of capability"],
  ["achievement_strength", "Achievement strength", "Impact & outcomes"],
  ["clarity_readability", "Clarity", "Easy to scan"],
  ["structure_completeness", "Structure", "Story completeness"],
  ["ats_compatibility", "ATS compatibility", "Parser friendly"],
] as const;

type HistoryItem = { id: string; createdAt: string; targetRole: string; result: ResumeAnalysis };

function loadHistory(): HistoryItem[] {
  return parseHistory<HistoryItem>(sessionStorage.getItem(HISTORY_KEY));
}
function scoreColor(score: number) { return score >= 80 ? "text-emerald-600" : score >= 60 ? "text-amber-600" : "text-rose-600"; }
function priorityClass(priority: string) { return priority.toLowerCase() === "high" ? "bg-rose-100 text-rose-700" : priority.toLowerCase() === "medium" ? "bg-amber-100 text-amber-700" : "bg-sky-100 text-sky-700"; }

export default function Home() {
  const [resumeText, setResumeText] = useState("");
  const [jobDescription, setJobDescription] = useState("");
  const [targetRole, setTargetRole] = useState("");
  const [activeTab, setActiveTab] = useState("analyze");
  const [result, setResult] = useState<ResumeAnalysis | null>(null);
  const [history, setHistory] = useState<HistoryItem[]>(() => loadHistory());
  const [mobileMenuOpen, setMobileMenuOpen] = useState(false);
  const fileRef = useRef<HTMLInputElement>(null);
  const analyze = trpc.resume.analyze.useMutation({
    onSuccess: (data) => {
      setResult(data); setActiveTab("results");
      const next = [{ id: crypto.randomUUID(), createdAt: new Date().toISOString(), targetRole: targetRole || data.target_role || "General review", result: data }, ...history].slice(0, 12);
      setHistory(next); sessionStorage.setItem(HISTORY_KEY, serializeHistory(next));
      toast.success("Your resume review is ready.");
    },
    onError: (error) => {
      // Check if error is due to missing backend configuration
      const errorMessage = error.message || "Analysis failed. Please try again.";
      if (errorMessage.includes("fetch") || errorMessage.includes("network") || errorMessage.includes("Unexpected token")) {
        toast.error("Backend not configured. Please set VITE_BACKEND_URL environment variable or deploy the backend server.");
      } else {
        toast.error(errorMessage);
      }
    },
  });

  const score = result?.overall_score ?? 0;
  const readiness = useMemo(() => score >= 80 ? "Strong foundation" : score >= 60 ? "Promising, with room to grow" : "Start with the essentials", [score]);

  async function extractFile(file: File) {
    try {
      if (file.type === "application/pdf" || file.name.toLowerCase().endsWith(".pdf")) {
        const buffer = await file.arrayBuffer();
        const pdf = await pdfjsLib.getDocument({ data: buffer }).promise;
        let text = "";
        for (let i = 1; i <= pdf.numPages; i++) { const page = await pdf.getPage(i); const content = await page.getTextContent(); text += `${content.items.map((item: any) => item.str).join(" ")}\n`; }
        setResumeText(text.trim());
      } else if (file.name.toLowerCase().endsWith(".docx")) {
        const buffer = await file.arrayBuffer();
        const extracted = await mammoth.extractRawText({ arrayBuffer: buffer });
        setResumeText(extracted.value.trim());
      } else throw new Error("Please choose a PDF or DOCX file.");
      toast.success("Resume text extracted. You can edit it before analyzing.");
    } catch (error) { toast.error(error instanceof Error ? error.message : "Could not read this file."); }
  }

  function startAnalyzing() { document.getElementById("analyzer")?.scrollIntoView({ behavior: "smooth" }); }
  function submit() { if (resumeText.trim().length < 80) return toast.error("Add at least 80 characters of resume text to begin."); analyze.mutate({ resumeText: resumeText.trim(), jobDescription: jobDescription.trim() || undefined, targetRole: targetRole.trim() || undefined }); }
  function restore(item: HistoryItem) { setResult(item.result); setTargetRole(item.targetRole === "General review" ? "" : item.targetRole); setActiveTab("results"); }

  return <div className="min-h-screen bg-[#f7f8fc] text-slate-900">
    <header className="sticky top-0 z-40 border-b border-slate-200/70 bg-[#f7f8fc]/90 backdrop-blur-xl">
      <div className="container flex h-20 items-center justify-between">
        <a href="#top" className="group flex items-center gap-3" aria-label="ResumeInsight AI home"><span className="logo-mark" aria-hidden="true"><span className="logo-mark__orbit"/><span className="logo-mark__core"><Sparkles size={16}/></span><span className="logo-mark__signal"/></span><span className="font-display text-lg font-semibold tracking-tight">ResumeInsight<span className="text-indigo-600"> AI</span></span></a>
        <nav className="hidden items-center gap-8 text-sm font-medium text-slate-500 md:flex"><a href="#how-it-works" className="transition hover:text-slate-900">How It Works</a><a href="#features" className="transition hover:text-slate-900">Capabilities</a><a href="#analyzer" className="transition hover:text-slate-900">Analyze</a><a href="#" className="transition hover:text-slate-900">Pricing</a><a href="#" className="transition hover:text-slate-900">Sign In</a></nav>
        <div className="flex items-center gap-4 md:hidden"><Button onClick={startAnalyzing} className="rounded-full bg-slate-900 px-4 text-xs font-semibold shadow-lg shadow-slate-900/15 hover:bg-indigo-700">Analyze <ArrowRight size={13}/></Button><button onClick={() => setMobileMenuOpen(!mobileMenuOpen)} className="p-2 text-slate-600 hover:text-slate-900" aria-label="Toggle menu"><Menu size={24}/></button></div>
        <div className="hidden md:block"><Button onClick={startAnalyzing} className="rounded-full bg-slate-900 px-5 text-xs font-semibold shadow-lg shadow-slate-900/15 hover:bg-indigo-700">Analyze Resume <ArrowRight size={15}/></Button></div>
      </div>
      {mobileMenuOpen && <div className="md:hidden border-t border-slate-200 bg-white py-4"><nav className="container flex flex-col gap-4 text-sm font-medium text-slate-600"><a href="#how-it-works" onClick={() => setMobileMenuOpen(false)} className="transition hover:text-slate-900">How It Works</a><a href="#features" onClick={() => setMobileMenuOpen(false)} className="transition hover:text-slate-900">Capabilities</a><a href="#analyzer" onClick={() => setMobileMenuOpen(false)} className="transition hover:text-slate-900">Analyze</a><a href="#" onClick={() => setMobileMenuOpen(false)} className="transition hover:text-slate-900">Pricing</a><a href="#" onClick={() => setMobileMenuOpen(false)} className="transition hover:text-slate-900">Sign In</a></nav></div>}
    </header>

    <main id="top">
      <section className="hero-grid relative overflow-hidden"><div className="container relative z-10 grid items-center gap-16 py-24 lg:grid-cols-[1.05fr_.95fr] lg:py-32"><div><div className="mb-6 inline-flex items-center gap-2 rounded-full border border-indigo-200 bg-white/80 px-3 py-1.5 text-xs font-semibold text-indigo-700 shadow-sm"><span className="size-1.5 rounded-full bg-indigo-500"/> AI-Powered Resume Analysis</div><h1 className="font-display text-5xl font-semibold leading-[1.02] tracking-[-.055em] text-slate-950 sm:text-7xl">Make your resume <span className="text-indigo-600">impossible to overlook.</span></h1><p className="mt-7 max-w-xl text-lg leading-8 text-slate-500">Upload your resume, understand your ATS score, discover skill gaps, match your resume to jobs, and get actionable improvements in seconds.</p><div className="mt-9 flex flex-wrap gap-3"><Button onClick={startAnalyzing} size="lg" className="h-13 rounded-full bg-indigo-600 px-7 font-semibold shadow-xl shadow-indigo-600/20 hover:bg-indigo-700">Analyze My Resume <ArrowRight size={17}/></Button><a href="#analyzer" className="inline-flex h-13 items-center rounded-full border border-slate-200 bg-white px-6 text-sm font-semibold text-slate-700 transition hover:border-slate-300">View Sample Report</a></div><div className="mt-9 flex items-center gap-6 text-xs font-medium text-slate-400"><span className="flex items-center gap-2"><LockKeyhole size={14}/> Private by design</span><span className="flex items-center gap-2"><Zap size={14}/> Results in seconds</span><span className="flex items-center gap-2"><FileText size={14}/> PDF & DOCX supported</span></div></div><div className="relative mx-auto w-full max-w-lg"><div className="absolute -inset-10 rounded-full bg-indigo-300/20 blur-3xl"/><Card className="relative overflow-hidden rounded-[2rem] border-white bg-white/90 shadow-2xl shadow-indigo-900/10"><CardHeader className="border-b border-slate-100 pb-5"><div className="flex items-center justify-between"><div><p className="text-[11px] font-semibold uppercase tracking-[.14em] text-slate-400">Sample Analysis</p><h3 className="font-display text-2xl font-semibold">Resume Score</h3></div><div className="flex items-center gap-2"><Badge className="rounded-full bg-emerald-50 text-emerald-700">Strong foundation</Badge></div></div></CardHeader><CardContent><div className="flex items-end gap-4"><div className="font-display text-6xl font-semibold tracking-[-.07em] text-emerald-600">82</div><div className="pb-2 text-sm text-slate-400">/ 100</div></div><div className="mt-6 space-y-3"><div className="flex items-center justify-between text-sm"><span className="text-slate-600">ATS Compatibility</span><span className="font-semibold text-emerald-600">92%</span></div><Progress value={92} className="h-2 bg-slate-100 [&>div]:bg-emerald-500"/><div className="flex items-center justify-between text-sm"><span className="text-slate-600">Job Alignment</span><span className="font-semibold text-emerald-600">88%</span></div><Progress value={88} className="h-2 bg-slate-100 [&>div]:bg-emerald-500"/><div className="flex items-center justify-between text-sm"><span className="text-slate-600">Skills Evidence</span><span className="font-semibold text-amber-600">76%</span></div><Progress value={76} className="h-2 bg-slate-100 [&>div]:bg-amber-500"/><div className="flex items-center justify-between text-sm"><span className="text-slate-600">Achievement Strength</span><span className="font-semibold text-rose-600">64%</span></div><Progress value={64} className="h-2 bg-slate-100 [&>div]:bg-rose-500"/></div><div className="mt-6 rounded-xl bg-indigo-50 p-4"><div className="flex items-start gap-3"><Sparkles className="size-5 text-indigo-600 shrink-0"/><div><p className="text-sm font-semibold text-indigo-900">One high-impact opportunity</p><p className="mt-1 text-xs leading-5 text-indigo-700">Turn responsibilities into measurable achievements.</p></div></div></div></CardContent></Card></div></div></section>

      <section id="features" className="container py-24"><div className="max-w-xl"><p className="eyebrow">Everything you need to build a stronger resume</p><h2 className="section-title mt-3">AI-powered resume analysis tools</h2></div><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-3"><Feature icon={<FileCheck/>} title="ATS Score" text="Understand how well your resume performs against applicant tracking systems."/><Feature icon={<Target/>} title="Job Match" text="Compare your resume against a specific job description."/><Feature icon={<AlertCircle/>} title="Skill Gap Detection" text="Discover important skills missing from your resume."/><Feature icon={<Search/>} title="Keyword Analysis" text="Identify keywords recruiters and ATS systems are looking for."/><Feature icon={<TrendingUp/>} title="Achievement Analysis" text="Find weak statements and turn them into measurable achievements."/><Feature icon={<Lightbulb/>} title="AI Resume Improvements" text="Get clear, practical suggestions for improving your resume."/></div></section>

      <section id="how-it-works" className="border-y border-slate-200/70 bg-white"><div className="container grid gap-12 py-24 lg:grid-cols-[.8fr_1.2fr] lg:items-center"><div><p className="eyebrow">How ResumeInsight AI works</p><h2 className="section-title mt-3">From resume upload to actionable improvements in four simple steps.</h2><p className="mt-5 leading-7 text-slate-500">Upload your resume, let our AI analyze it, get your insights, and apply improvements to strengthen your resume.</p></div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{[["01","Upload Resume","Upload your PDF or DOCX resume."],["02","AI Analysis","Our AI evaluates your resume structure, skills, keywords, and achievements."],["03","Get Your Insights","See your score, job alignment, skill gaps, and improvement opportunities."],["04","Improve & Apply","Use actionable suggestions to strengthen your resume."]].map(([n,t,d])=><div key={n} className="rounded-3xl border border-slate-200 bg-[#fbfcff] p-5 transition hover:-translate-y-1 hover:shadow-lg"><span className="font-display text-3xl font-semibold text-indigo-600">{n}</span><h3 className="mt-10 font-semibold">{t}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{d}</p></div>)}</div></div></section>

      <section id="analyzer" className="container py-24"><div className="mb-10 flex flex-wrap items-end justify-between gap-5"><div><p className="eyebrow">Your workspace</p><h2 className="section-title mt-3">Bring your resume into focus.</h2></div></div><div className="mb-8 flex flex-wrap gap-6 rounded-2xl bg-indigo-50 p-6 md:gap-8"><div className="flex items-center gap-3"><LockKeyhole size={18} className="text-indigo-600"/><div><p className="font-semibold text-indigo-900">Private by design</p><p className="text-sm text-indigo-700">Your resume is handled securely.</p></div></div><div className="flex items-center gap-3"><Zap size={18} className="text-indigo-600"/><div><p className="font-semibold text-indigo-900">Fast analysis</p><p className="text-sm text-indigo-700">Get useful insights in seconds.</p></div></div><div className="flex items-center gap-3"><FileText size={18} className="text-indigo-600"/><div><p className="font-semibold text-indigo-900">Multiple formats</p><p className="text-sm text-indigo-700">Upload PDF or DOCX resumes.</p></div></div></div><Tabs value={activeTab} onValueChange={setActiveTab}><TabsList className="mb-6 rounded-full bg-slate-200/70 p-1"><TabsTrigger value="analyze" className="rounded-full px-5 data-[state=active]:bg-white data-[state=active]:shadow-sm">Analyze</TabsTrigger><TabsTrigger value="results" disabled={!result} className="rounded-full px-5 data-[state=active]:bg-white data-[state=active]:shadow-sm">Results {result && <span className="ml-2 size-1.5 rounded-full bg-indigo-500"/>}</TabsTrigger><TabsTrigger value="history" className="rounded-full px-5 data-[state=active]:bg-white data-[state=active]:shadow-sm"><History size={14} className="mr-2"/> History {history.length > 0 && `(${history.length})`}</TabsTrigger></TabsList><TabsContent value="analyze"><div className="grid gap-5 lg:grid-cols-[1.15fr_.85fr]"><Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><div className="flex items-start justify-between gap-3"><div><CardTitle className="font-display text-2xl">Your resume</CardTitle><p className="mt-1 text-sm text-slate-500">Paste it in or upload a document. We’ll extract the text locally.</p></div><Badge variant="outline" className="rounded-full border-emerald-200 bg-emerald-50 text-emerald-700"><LockKeyhole size={12} className="mr-1"/> Local extraction</Badge></div></CardHeader><CardContent><button type="button" aria-label="Upload a PDF or DOCX resume" onClick={() => fileRef.current?.click()} className="group mb-4 flex w-full cursor-pointer items-center justify-between rounded-2xl border border-dashed border-slate-300 bg-slate-50/70 px-4 py-3 text-left transition hover:border-indigo-400 hover:bg-indigo-50/40"><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-white text-indigo-600 shadow-sm"><UploadCloud size={17}/></span><div><p className="text-sm font-semibold">Upload PDF or DOCX</p><p className="text-xs text-slate-400">Text is extracted in your browser</p></div></div><span className="text-xs font-semibold text-indigo-600">Browse</span><input ref={fileRef} type="file" accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" className="hidden" onChange={e=>{const file=e.target.files?.[0]; if(file) extractFile(file); e.currentTarget.value=""}}/></button><Textarea value={resumeText} onChange={e=>setResumeText(e.target.value)} placeholder="Paste your resume text here…\n\nInclude your experience, projects, education, skills, and achievements for the most useful review." className="min-h-[360px] resize-none rounded-2xl border-slate-200 bg-white p-5 text-sm leading-6 shadow-none focus-visible:ring-indigo-500"/><div className="mt-3 flex items-center justify-between text-xs text-slate-400"><span>{resumeText.length.toLocaleString()} characters</span>{resumeText && <button className="flex items-center gap-1 text-slate-500 hover:text-rose-600" onClick={()=>setResumeText("")}><X size={13}/> Clear</button>}</div></CardContent></Card><Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><CardTitle className="font-display text-2xl">Add direction <span className="font-sans text-sm font-normal text-slate-400">(optional)</span></CardTitle><p className="mt-1 text-sm text-slate-500">A target role unlocks sharper matching and keyword guidance.</p></CardHeader><CardContent className="space-y-5"><div><label className="mb-2 block text-xs font-semibold uppercase tracking-[.12em] text-slate-500">Target role</label><Input value={targetRole} onChange={e=>setTargetRole(e.target.value)} placeholder="e.g. Product Designer" className="h-12 rounded-xl border-slate-200"/></div><div><label className="mb-2 block text-xs font-semibold uppercase tracking-[.12em] text-slate-500">Job description</label><Textarea value={jobDescription} onChange={e=>setJobDescription(e.target.value)} placeholder="Paste the role description to compare requirements…" className="min-h-[225px] resize-none rounded-xl border-slate-200 leading-6"/></div><div className="rounded-2xl bg-indigo-50 p-4 text-xs leading-5 text-indigo-800"><div className="flex gap-2"><Sparkles size={14} className="mt-0.5 shrink-0"/><span>We evaluate the resume evidence—not personal characteristics. Every suggested edit should remain truthful.</span></div></div><Button onClick={submit} disabled={analyze.isPending} className="h-12 w-full rounded-xl bg-slate-900 font-semibold hover:bg-indigo-700">{analyze.isPending ? <><Loader2 size={16} className="mr-2 animate-spin"/> Reviewing your resume…</> : <>Run my analysis <ArrowRight size={16} className="ml-2"/></>}</Button></CardContent></Card></div></TabsContent><TabsContent value="results">{result && <Results result={result} readiness={readiness} score={score}/>}</TabsContent><TabsContent value="history"><HistoryPanel history={history} onRestore={restore} onClear={()=>{setHistory([]);sessionStorage.removeItem(HISTORY_KEY);toast.success("History cleared.")}}/></TabsContent></Tabs>      </section>

      <section className="container py-24 text-center"><div className="max-w-2xl mx-auto"><h2 className="font-display text-4xl font-semibold tracking-tight text-slate-950 sm:text-5xl">Your next opportunity starts with a better resume.</h2><p className="mt-5 text-lg leading-8 text-slate-500">Get clear, evidence-based feedback and know exactly what to improve.</p><Button onClick={startAnalyzing} size="lg" className="mt-9 h-13 rounded-full bg-indigo-600 px-8 font-semibold shadow-xl shadow-indigo-600/20 hover:bg-indigo-700">Analyze My Resume <ArrowRight size={17}/></Button></div></section>
    </main><footer className="border-t border-slate-200/70 bg-white"><div className="container py-12"><div className="grid gap-8 md:grid-cols-4 lg:grid-cols-5"><div className="md:col-span-2 lg:col-span-2"><div className="flex items-center gap-3 mb-4"><span className="logo-mark" aria-hidden="true"><span className="logo-mark__orbit"/><span className="logo-mark__core"><Sparkles size={16}/></span><span className="logo-mark__signal"/></span><span className="font-display text-lg font-semibold tracking-tight">ResumeInsight<span className="text-indigo-600"> AI</span></span></div><p className="text-sm text-slate-500">Evidence-based career clarity.</p></div><div><p className="font-semibold text-slate-900 mb-4">Product</p><ul className="space-y-3 text-sm text-slate-500"><li><a href="#how-it-works" className="transition hover:text-slate-900">How It Works</a></li><li><a href="#features" className="transition hover:text-slate-900">Capabilities</a></li><li><a href="#analyzer" className="transition hover:text-slate-900">Analyze Resume</a></li></ul></div><div><p className="font-semibold text-slate-900 mb-4">Resources</p><ul className="space-y-3 text-sm text-slate-500"><li><a href="#" className="transition hover:text-slate-900">Resume Guide</a></li><li><a href="#" className="transition hover:text-slate-900">ATS Guide</a></li><li><a href="#" className="transition hover:text-slate-900">Career Tips</a></li></ul></div><div><p className="font-semibold text-slate-900 mb-4">Company</p><ul className="space-y-3 text-sm text-slate-500"><li><a href="#" className="transition hover:text-slate-900">About</a></li><li><a href="#" className="transition hover:text-slate-900">Contact</a></li><li><a href="#" className="transition hover:text-slate-900">Privacy</a></li><li><a href="#" className="transition hover:text-slate-900">Terms</a></li></ul></div></div><div className="mt-12 flex flex-wrap items-center justify-between gap-4 border-t border-slate-200 pt-8 text-xs text-slate-400"><span> 2026 ResumeInsight AI</span><span className="flex items-center gap-2"><LockKeyhole size={13}/> Private by design  AI-assisted, not a hiring decision</span></div></div></footer>
  </div>;
}

function Feature({icon,title,text}:{icon:React.ReactNode;title:string;text:string}) { return <Card className="rounded-3xl border-slate-200/80 bg-white p-2 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><CardContent className="p-6"><span className="grid size-11 place-items-center rounded-2xl bg-indigo-50 text-indigo-600">{icon}</span><h3 className="mt-7 font-display text-xl font-semibold">{title}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{text}</p></CardContent></Card> }

function Results({result,readiness,score}:{result:ResumeAnalysis;readiness:string;score:number}) { const scores=result.section_scores; return <div className="space-y-5"><div className="grid gap-5 lg:grid-cols-[.75fr_1.25fr]"><Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardContent className="flex h-full flex-col justify-between p-7"><div><p className="eyebrow">Analysis complete</p><h3 className="mt-2 font-display text-2xl font-semibold">{readiness}</h3><p className="mt-3 text-sm leading-6 text-slate-500">{result.executive_summary}</p></div><div className="mt-8 flex items-end gap-4"><div className={`font-display text-7xl font-semibold tracking-[-.07em] ${scoreColor(score)}`}>{score}</div><div className="pb-2 text-sm text-slate-400">/ 100<br/><span className="font-semibold text-slate-600">{result.score_label}</span></div></div><Progress value={score} className="mt-5 h-2 bg-slate-100 [&>div]:bg-indigo-600"/><p className="mt-3 text-xs text-slate-400">Confidence: {Math.round(result.confidence*100)}%  AI-assisted resume quality indicator</p></CardContent></Card><Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><CardTitle className="font-display text-xl">Six dimensions, one clear picture</CardTitle><p className="text-sm text-slate-500">See what is carrying your application—and what can move it forward.</p></CardHeader><CardContent className="grid grid-cols-2 gap-4 md:grid-cols-3">{dimensions.map(([key,label,sub])=>{const val=scores[key];return <div key={key} className="rounded-2xl bg-slate-50 p-4"><div className="flex items-start justify-between gap-2"><div><p className="text-sm font-semibold text-slate-700">{label}</p><p className="mt-1 text-xs text-slate-400">{sub}</p></div><strong className={`text-lg ${scoreColor(val)}`}>{val}</strong></div><Progress value={val} className="mt-4 h-1.5 bg-slate-200 [&>div]:bg-indigo-500"/></div>})}</CardContent></Card></div><div className="grid gap-5 lg:grid-cols-2"><Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><CardTitle className="font-display text-xl">What’s working</CardTitle></CardHeader><CardContent className="space-y-3">{result.strengths.length ? result.strengths.map((item,i)=><div key={i} className="rounded-2xl border border-emerald-100 bg-emerald-50/50 p-4"><div className="flex gap-3"><span className="mt-0.5 grid size-6 place-items-center rounded-full bg-emerald-500 text-white"><Check size={13}/></span><div><p className="font-semibold text-emerald-950">{item.title}</p><p className="mt-1 text-sm leading-6 text-emerald-900/70">{item.explanation}</p>{item.evidence&&<blockquote className="mt-3 border-l-2 border-emerald-300 pl-3 text-xs italic text-emerald-800/70">“{item.evidence}”</blockquote>}</div></div></div>) : <Empty text="No strengths were returned for this review."/>}</CardContent></Card><Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><CardTitle className="font-display text-xl">Highest-impact improvements</CardTitle></CardHeader><CardContent className="space-y-3">{result.areas_for_improvement.length ? result.areas_for_improvement.slice(0,5).map((item,i)=><div key={i} className="rounded-2xl border border-slate-200 p-4"><div className="flex items-center justify-between gap-3"><Badge className={`rounded-full border-0 text-[10px] uppercase tracking-wider ${priorityClass(item.priority)}`}>{item.priority} priority</Badge><span className="text-xs text-slate-400">{item.category}</span></div><p className="mt-3 font-semibold text-slate-800">{item.issue}</p><p className="mt-1 text-sm leading-6 text-slate-500">{item.recommended_action}</p>{item.evidence && <blockquote className="mt-2 border-l-2 border-slate-300 pl-3 text-xs italic text-slate-400">“{item.evidence}”</blockquote>}</div>) : <Empty text="Your resume is already looking focused here."/>}</CardContent></Card></div><KeywordCard data={result.keyword_analysis}/><div className="grid gap-5 lg:grid-cols-[1.1fr_.9fr]"><RewriteCard items={result.bullet_point_feedback}/><ActionCard items={result.action_plan}/></div></div> }
function KeywordCard({data}:{data:ResumeAnalysis["keyword_analysis"]}) { return <Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><CardTitle className="font-display text-xl">Keyword lens</CardTitle><p className="text-sm text-slate-500">Use missing terms only when they genuinely describe your experience.</p></CardHeader><CardContent className="grid gap-5 md:grid-cols-3">{[["Matched",data.matched_keywords,"keyword-matched"],["Missing",data.missing_keywords,"keyword-missing"],["Overused / weak",data.overused_or_weak_keywords,"keyword-overused"]].map(([label,items,cls])=><div key={label as string}><p className="mb-3 text-xs font-semibold uppercase tracking-[.14em] text-slate-400">{label}</p><div className="flex flex-wrap gap-2">{(items as string[]).length ? (items as string[]).map(x=><span key={x} className={`rounded-full px-3 py-1.5 text-xs font-medium ${cls}`}>{x}</span>) : <span className="text-sm text-slate-400">None identified</span>}</div></div>)}</CardContent></Card> }
function RewriteCard({items}:{items:ResumeAnalysis["bullet_point_feedback"]}) { return <Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><CardTitle className="font-display text-xl">Sharper bullet points</CardTitle></CardHeader><CardContent className="space-y-4">{items.length ? items.slice(0,3).map((x,i)=><div key={i} className="overflow-hidden rounded-2xl border border-slate-200"><div className="grid md:grid-cols-2"><div className="bg-slate-50 p-4"><p className="mb-2 text-[10px] font-bold uppercase tracking-wider text-slate-400">Original</p><p className="text-sm leading-6 text-slate-600">{x.original_text}</p></div><div className="bg-indigo-50/60 p-4"><p className="mb-2 text-[10px] font-bold uppercase tracking-wider text-indigo-500">Suggested version</p><p className="text-sm leading-6 text-indigo-950">{x.suggested_version}</p></div></div></div>) : <Empty text="No bullet rewrites were needed for this review."/>}</CardContent></Card> }
function ActionCard({items}:{items:ResumeAnalysis["action_plan"]}) { return <Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader><CardTitle className="font-display text-xl">Your action plan</CardTitle></CardHeader><CardContent className="space-y-3">{items.length ? items.slice(0,5).map((x,i)=><div key={i} className="flex gap-3 rounded-2xl bg-slate-50 p-4"><span className="grid size-7 shrink-0 place-items-center rounded-full bg-slate-900 text-xs font-bold text-white">{x.order || i+1}</span><div><p className="text-sm font-semibold text-slate-800">{x.action}</p><p className="mt-1 text-xs leading-5 text-slate-500"><span className="font-semibold text-indigo-600">Benefit:</span> {x.expected_benefit}</p></div></div>) : <Empty text="Your plan will appear here after analysis."/>}</CardContent></Card> }
function HistoryPanel({history,onRestore,onClear}:{history:HistoryItem[];onRestore:(i:HistoryItem)=>void;onClear:()=>void}) { return <Card className="rounded-3xl border-slate-200/80 shadow-sm"><CardHeader className="flex-row items-center justify-between"><div><CardTitle className="font-display text-2xl">Recent reviews</CardTitle><p className="mt-1 text-sm text-slate-500">Stored locally in this browser. No account required.</p></div>{history.length>0&&<Button onClick={onClear} variant="ghost" className="text-xs text-slate-400 hover:text-rose-600">Clear history</Button>}</CardHeader><CardContent>{history.length ? <div className="space-y-3">{history.map(item=><button key={item.id} onClick={()=>onRestore(item)} className="flex w-full items-center justify-between rounded-2xl border border-slate-200 p-4 text-left transition hover:border-indigo-300 hover:bg-indigo-50/40"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-indigo-50 text-indigo-600"><FileText size={17}/></span><div><p className="font-semibold text-slate-800">{item.targetRole}</p><p className="mt-1 text-xs text-slate-400">{new Date(item.createdAt).toLocaleString()}</p></div></div><div className="flex items-center gap-3"><strong className={`font-display text-2xl ${scoreColor(item.result.overall_score)}`}>{item.result.overall_score}</strong><ChevronDown size={16} className="-rotate-90 text-slate-300"/></div></button>)}</div> : <div className="grid place-items-center py-20 text-center"><span className="grid size-14 place-items-center rounded-2xl bg-slate-100 text-slate-400"><History/></span><p className="mt-4 font-semibold">Your review history is empty</p><p className="mt-1 text-sm text-slate-400">Run your first analysis to see it here.</p></div>}</CardContent></Card> }
function Empty({text}:{text:string}) { return <p className="rounded-2xl bg-slate-50 p-5 text-sm text-slate-400">{text}</p> }
